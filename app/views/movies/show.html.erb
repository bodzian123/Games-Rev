<p>
  <strong>Title:</strong>
  <%= @movie.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @movie.text %>
</p>

<h2>Comments</h2>
<%= render @movie.comentages %>

<h2>Add a comment:</h2>
<%= render 'comentages/form' %>

<%= link_to 'Edit', edit_movie_path(@movie) %>
<%= link_to 'Back', movies_path %>
