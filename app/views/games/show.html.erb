<p>
  <strong>Title:</strong>
  <%= @game.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @game.text %>
</p>

<h2>Comments</h2>
<%= render @game.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_game_path(@game) %>
<%= link_to 'Back', games_path %>
